# Manifests

This directory contains generic Cloud Foundry manifest examples for API, UI,
Node.js buildpack, and Memcached scenarios. They are local-development templates,
not production deployment configuration. Replace every `((variable))` value and
review resource limits, routes, security, and service bindings before use.
